import django.db.models.signals
import django.dispatch
import catalog.models


# @django.dispatch.receiver(django.db.models.signals.post_save, sender=models.<somemodel>,)
# def send(sender, instance, created, **kwargs,):
#     pass
